{
  "name": "custos-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "build:backend": "tsc",
    "build:frontend": "npm --prefix frontend run build",
    "build": "npm run build:backend && npm run build:frontend",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "playwright": "^1.61.1",
    "typescript": "^5.7.2",
    "vitest": "^3.0.2"
  },
  "dependencies": {
    "@platform/contract": "file:../HUB/packages/contract",
    "@fastify/cors": "^11.1.0",
    "@fastify/static": "^9.1.3",
    "better-sqlite3": "^12.11.1",
    "fastify": "^5.6.2"
  }
}
